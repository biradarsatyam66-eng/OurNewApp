import { LoweCaseDirective } from './lowe-case.directive';

describe('LoweCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new LoweCaseDirective();
    expect(directive).toBeTruthy();
  });
});
